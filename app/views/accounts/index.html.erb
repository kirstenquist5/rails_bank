
<%= button_to "log out", destroy_user_session_path, method: :delete %>
<h1><%= current_user.first_name  %>Account</h1>

# user_signed_in? - check to see if the user is logged on
# User_session - access to the current user User_session

<%= link_to 'Add Account', new_account_path %>
<h3>All Accounts</h3>

<ul>
  <% @accounts.each do |account| %>
    <li>
    <%= account.name %> - <%= account.balance %>
    </li>
</ul>